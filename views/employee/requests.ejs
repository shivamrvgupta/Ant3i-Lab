<!doctype html>
<html lang="en">
<head><%- include('../partials/head') %><title>Booking Requests</title></head>
<body class="app-bg">
<%- include('../partials/navbar') %>

<div class="container-xxl py-4">
  <div class="d-flex align-items-center gap-3 mb-4">
    <a href="/employee/dashboard" class="btn btn-sm btn-outline-secondary">← Back</a>
    <h4 class="fw-bold mb-0">Booking Requests</h4>
    <span class="badge bg-warning text-dark">
      <%= requests.filter(r => r.status === 'pending').length %> Pending
    </span>
  </div>

  <div class="card border-0 rounded-4 shadow-sm">
    <div class="card-body">
      <% if (requests.length === 0) { %>
        <div class="text-muted small">No booking requests yet.</div>
      <% } else { %>
      <div class="table-responsive">
        <table class="table table-sm table-hover align-middle mb-0">
          <thead class="table-light">
            <tr>
              <th>Req No.</th>
              <th>Customer</th>
              <th>Tests</th>
              <th>Date</th>
              <th>Amount</th>
              <th>Status</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% requests.forEach(r => { %>
            <tr>
              <td class="font-monospace small"><%= r.requestNo %></td>
              <td>
                <div class="fw-semibold small"><%= r.name %></div>
                <% if (r.company) { %><div class="text-muted" style="font-size:.75rem"><%= r.company %></div><% } %>
              </td>
              <td class="small text-muted"><%= r.selectedTests.length %> test<%= r.selectedTests.length > 1 ? 's' : '' %></td>
              <td class="small"><%= new Date(r.preferredDate).toLocaleDateString('en-IN', { day:'2-digit', month:'short', year:'numeric' }) %></td>
              <td class="small">
                <div>₹<%= r.totalAmount.toLocaleString('en-IN') %></div>
                <form method="POST" action="/employee/requests/<%= r._id %>/payment-toggle" style="display:inline">
                  <button class="btn px-1 py-0 mt-1 <%= r.paymentStatus === 'paid' ? 'btn-success' : 'btn-outline-warning' %>"
                    style="font-size:.7rem" type="submit">
                    <%= r.paymentStatus === 'paid' ? '✓ Paid' : 'Unpaid' %>
                  </button>
                </form>
              </td>
              <td>
                <span class="badge <%=
                  r.status === 'pending'  ? 'bg-warning text-dark' :
                  r.status === 'accepted' ? 'bg-success' : 'bg-danger'
                %>"><%= r.status.charAt(0).toUpperCase() + r.status.slice(1) %></span>
              </td>
              <td class="d-flex gap-1 flex-wrap">
                <% if (r.status === 'pending') { %>
                  <form method="POST" action="/employee/requests/<%= r._id %>/accept" style="display:inline">
                    <button class="btn btn-sm btn-success" type="submit">Accept &amp; Create Report</button>
                  </form>
                  <button class="btn btn-sm btn-outline-danger"
                    data-bs-toggle="modal" data-bs-target="#rejectModal"
                    data-id="<%= r._id %>" data-name="<%= r.name %>">Reject</button>
                <% } else if (r.status === 'accepted' && r.reportId) { %>
                  <a href="/employee/reports/<%= r.reportId %>" class="btn btn-sm btn-outline-secondary">View Report</a>
                <% } else if (r.status === 'accepted') { %>
                  <a href="/employee/reports/new?from=<%= r._id %>" class="btn btn-sm btn-outline-dark">Create Report</a>
                <% } %>
                <a href="/employee/requests/<%= r._id %>/invoice" class="btn btn-sm btn-outline-dark" target="_blank">Invoice</a>
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
      <% } %>
    </div>
  </div>
</div>

<!-- Reject Modal -->
<div class="modal fade" id="rejectModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content rounded-4">
      <div class="modal-header">
        <h5 class="modal-title">Reject Request</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <form method="POST" id="rejectForm">
        <div class="modal-body">
          <p class="small text-muted">Rejecting request from <strong id="rejectName"></strong>.</p>
          <label class="form-label small">Reason (optional)</label>
          <textarea class="form-control" name="rejectionNote" rows="3" placeholder="e.g. Lab unavailable on that date"></textarea>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-outline-secondary btn-sm" data-bs-dismiss="modal">Cancel</button>
          <button type="submit" class="btn btn-danger btn-sm">Confirm Reject</button>
        </div>
      </form>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
document.getElementById('rejectModal').addEventListener('show.bs.modal', e => {
  const btn  = e.relatedTarget;
  document.getElementById('rejectForm').action = `/employee/requests/${btn.dataset.id}/reject`;
  document.getElementById('rejectName').textContent = btn.dataset.name;
});
</script>
</body>
</html>
